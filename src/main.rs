use the_book::{Point, debug};

fn main() {
    // let p = Point::new(5, 10);
    // let squre = Point::sauere(10);
    // println!("x is {}, y is {}", &p.x(), &p.y());
    // println!("the squre is {:#?}", squre);
}
