[package]
name = "the_book"
version = "0.1.0"
edition = "2024"

[workspace]
resolver = "3"
members = ["add_one", "add_two", "adder"]

[dependencies]
rand = "0.9.2"
add_one = { path = "add_one" }
add_two = { path = "add_two" }
